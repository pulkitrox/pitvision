// PitVision AI is served as static HTML from /app/frontend/public.
// React is intentionally disabled.
const root = document.getElementById("root");
if (root) {
  // No-op: static HTML pages do not include a #root element.
}
